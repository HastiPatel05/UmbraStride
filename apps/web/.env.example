# Optional: Mapbox Streets instead of OpenFreeMap.
# Live building shadows use local SunCalc + building footprints; no ShadeMap key is required.
VITE_MAPBOX_ACCESS_TOKEN=

# API proxy target (vite dev server proxies /api -> localhost:8000)
# VITE_API_URL=/api

VITE_DEFAULT_AOI=az-phoenix
